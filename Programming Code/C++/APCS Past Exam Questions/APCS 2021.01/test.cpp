#include<iostream>
#include<iomanip>
#include<vector>
#include<queue>
#include<stack>
#include<algorithm>
using namespace std;
#define opt ios_base::sync_with_stdio(false); cin.tie(0); cout.tie(0);

const int MAXN = 1e3 + 50;
const int Mod = 1e9 + 7;

signed main(){
    opt;
    int q[MAXN][MAXN];
    int n , m , k;
    cin >> n >> m >> k;
    for( int i = 0 ; i < n ; i++ ){
        for( int j = 0 ; j < m ; j++ ){
            cin >> q[i][j];
        }
    }
    for( int i = 0 ; i < k ; i++ ){
        for( int j = 0 ; j < n ; j++ ){
            
        }
    }
}